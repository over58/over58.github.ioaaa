"use strict";var exec=require("child_process").execSync,chalk=require("chalk"),ora=require("ora"),spinner=ora("start...").start(),commands=["git add .",'git commit -m "update artcile"',"git push origin myblog"];commands.forEach(function(e,r){spinner.text="loading",exec(e)}),spinner.succeed(chalk.green("success!"));