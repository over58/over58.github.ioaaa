"use strict";var execSync=require("child_process").execSync,chalk=require("chalk"),ora=require("ora"),spinner=ora().start(chalk.bgGrey("start...")),commands=["git add .",'git commit -m "feat: update artcile"',"git push origin myblog"];spinner.text="loading",commands.forEach(function(e){execSync(e)}),spinner.succeed(chalk.green("success!"));